FitLife Fasting Patch 46

Installs the user-approved first phase illustration set and ensures that the 4h milestone remains locked before four elapsed hours.

Apply:
  cd /workspaces/FitLife_Full
  unzip -o FitLife-Fasting-Patch-46-Phase-Image-Assets.zip
  bash FitLife-Fasting-Patch-46-Phase-Image-Assets/install.sh

The installer creates a timestamped source backup and runs npm run build.

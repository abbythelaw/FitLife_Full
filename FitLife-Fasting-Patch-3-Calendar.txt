cd /workspaces/FitLife_Full
python3 - <<'PY'
from pathlib import Path
from textwrap import dedent
base=Path('src/features/fasting');page=base/'FastingPage.jsx';css=base/'FastingPage.css'
if not page.exists():raise SystemExit('Apply Patches 1 and 2 first.')
(base/'FastingCalendar.jsx').write_text(dedent(r'''
import {useMemo,useState} from 'react'
const key=d=>[d.getFullYear(),String(d.getMonth()+1).padStart(2,'0'),String(d.getDate()).padStart(2,'0')].join('-')
const hours=row=>Math.max(0,(new Date(row.ended_at||row.expected_end_at)-new Date(row.started_at))/36e5)
export default function FastingCalendar({sessions=[]}){const[range,setRange]=useState(30),[selected,setSelected]=useState(null);const completed=sessions.filter(x=>x.status==='completed'&&x.ended_at);const days=useMemo(()=>{const today=new Date();return Array.from({length:range},(_,i)=>{const d=new Date(today);d.setDate(today.getDate()-(range-1-i));const matches=completed.filter(x=>String(x.started_at).slice(0,10)===key(d));const best=[...matches].sort((a,b)=>hours(b)-hours(a))[0],actual=best?hours(best):0,target=Number(best?.target_hours||0);return{date:key(d),d,best,actual,state:!best?'empty':actual<target?'partial':actual>target?'exceeded':'complete'}})},[sessions,range]);let current=0;for(const d of [...days].reverse()){if(d.best)current++;else break}let best=0,n=0;for(const d of days){n=d.best?n+1:0;best=Math.max(best,n)}return <section className="fast-calendar-card"><header><div><small>FASTING CALENDAR</small><h2>Consistency</h2></div><div>{[30,90,365].map(v=><button className={range===v?'active':''} onClick={()=>setRange(v)} key={v}>{v===365?'1Y':`${v}D`}</button>)}</div></header><div className="fast-calendar-stats"><span><b>{current}</b>Current streak</span><span><b>{best}</b>Best streak</span><span><b>{completed.length}</b>Completed fasts</span></div><div className="fast-calendar-grid">{days.map(day=><button key={day.date} className={day.state} title={`${day.date}: ${day.actual.toFixed(1)}h`} onClick={()=>setSelected(day)}><i/><small>{day.d.getDate()}</small></button>)}</div><div className="fast-calendar-legend"><span><i className="empty"/>No fast</span><span><i className="partial"/>Partial</span><span><i className="complete"/>Target met</span><span><i className="exceeded"/>Exceeded</span></div>{selected&&<div className="fast-day-detail"><button onClick={()=>setSelected(null)}>Close</button><small>{selected.date}</small><h3>{selected.best?`${selected.actual.toFixed(1)} hour fast`:'No completed fast'}</h3>{selected.best&&<p>{selected.best.protocol} · Target {selected.best.target_hours}h · {selected.state==='partial'?'Ended early':selected.state==='exceeded'?'Exceeded target':'Target met'}</p>}</div>}</section>}
'''))
text=page.read_text();text=text.replace("import {listFasts,saveFast} from './fastingStore'","import {listFasts,saveFast} from './fastingStore'\nimport FastingCalendar from './FastingCalendar'");text=text.replace('<div id="fasting-calendar-slot"/>','<FastingCalendar sessions={sessions}/>');page.write_text(text)
css.write_text(css.read_text()+dedent(r'''
.fast-calendar-card{padding:22px;border:1px solid var(--line);border-radius:24px;background:var(--card)}.fast-calendar-card>header{display:flex;justify-content:space-between;align-items:end}.fast-calendar-card>header button{padding:8px 12px;border:1px solid var(--line);background:transparent;color:var(--text)}.fast-calendar-card>header button.active{background:var(--primary);color:#07110a}.fast-calendar-stats{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin:16px 0}.fast-calendar-stats span{display:grid;padding:13px;border-radius:14px;background:var(--card2);color:var(--muted)}.fast-calendar-stats b{font-size:25px;color:var(--text)}.fast-calendar-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(28px,1fr));gap:6px}.fast-calendar-grid button{display:grid;gap:4px;border:0;background:transparent;color:var(--muted)}.fast-calendar-grid i,.fast-calendar-legend i{height:22px;border-radius:6px;background:#313733}.fast-calendar-grid .partial i,.fast-calendar-legend .partial{background:#a98543}.fast-calendar-grid .complete i,.fast-calendar-legend .complete{background:#2fa46c}.fast-calendar-grid .exceeded i,.fast-calendar-legend .exceeded{background:#5de095}.fast-calendar-legend{display:flex;gap:12px;flex-wrap:wrap;margin-top:14px;font-size:10px}.fast-calendar-legend span{display:flex;gap:5px;align-items:center}.fast-calendar-legend i{width:12px;height:12px}.fast-day-detail{margin-top:16px;padding:16px;border:1px solid var(--line);border-radius:16px;background:var(--card2)}
'''))
print('[OK] Patch 3 installed')
PY
npm run build
